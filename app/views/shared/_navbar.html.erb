<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if user_signed_in? %>
        <%= link_to raw("Easy<strong>surveys</strong>"), surveys_path, class: "navbar-brand" %>
      <% else %>
        <%= link_to raw("Easy<strong>surveys</strong>"), root_path, class: "navbar-brand" %>
      <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">

      <% if user_signed_in? %>
      <li><%= link_to raw("<i class='fa fa-plus'> New Survey</i>"), new_survey_path %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><%= "#{current_user.first_name} #{current_user.last_name}" %> <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to "Surveys", surveys_path %></li>
            <li><%= link_to "Account", edit_user_registration_path %></li>
            <li class="divider"></li>
            <li><%= link_to "Log out", destroy_user_session_path, method: "DELETE" %></li>
          </ul>
        </li>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class: "btn btn-default navbar-btn" %>
        <%= link_to "Sign up", new_user_registration_path, class: "btn btn-primary navbar-btn" %>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, class: "alert alert-info" %>
<% end %>